#pragma once

#include <windows.h>
#include <windowsx.h>
#include <string>
#include <vector>

class CGlobalVariable
{
public:
	static char			acRootPath[MAX_PATH];
	static char			acProgPath[MAX_PATH];
};
